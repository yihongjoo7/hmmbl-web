// 개발 허브 → 멤버목록 (테스트) 진입점.
// 화면 구현은 features/_templates/member/MemberListPage.tsx 에 있다.
// (features/ 하위 파일은 라우트가 아니므로 이 app 라우트에서 re-export 한다.)
export { default } from '@/features/_templates/member/MemberListPage';
