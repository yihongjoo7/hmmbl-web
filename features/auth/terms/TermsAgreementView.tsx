/** [퍼블리셔] TermsAgreement 화면 레이아웃 — IA No.18-19 */
// 역할: 정적 안내·설정 화면.

interface TermsAgreementViewProps {
  // TODO: 필요한 props 정의
}

export function TermsAgreementView(_props: TermsAgreementViewProps) {
  return (
    <div className="flex flex-col gap-4 p-4">
      {/* TODO: 화면 UI 구현 */}
    </div>
  );
}
