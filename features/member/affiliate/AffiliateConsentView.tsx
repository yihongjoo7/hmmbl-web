/** [퍼블리셔] AffiliateConsent 화면 레이아웃 — IA No.93 */
// 역할: 정적 안내·설정 화면.

interface AffiliateConsentViewProps {
  // TODO: 필요한 props 정의
}

export function AffiliateConsentView(_props: AffiliateConsentViewProps) {
  return (
    <div className="flex flex-col gap-4 p-4">
      {/* TODO: 화면 UI 구현 */}
    </div>
  );
}
