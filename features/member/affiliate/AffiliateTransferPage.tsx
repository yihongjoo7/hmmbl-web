'use client';
/** [개발자] AffiliateTransfer Container — IA No.91 */
// 역할: 별도 데이터 로드 없이 UI만 렌더링합니다.
// TODO: View 이름과 import 경로를 실제 기능명으로 변경하세요.

import { AffiliateTransferView } from './AffiliateTransferView';

export default function AffiliateTransferPage() {
  return <AffiliateTransferView />;
}
