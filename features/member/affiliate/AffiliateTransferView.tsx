/** [퍼블리셔] AffiliateTransfer 화면 레이아웃 — IA No.91 */
// 역할: 정적 안내·설정 화면.

interface AffiliateTransferViewProps {
  // TODO: 필요한 props 정의
}

export function AffiliateTransferView(_props: AffiliateTransferViewProps) {
  return (
    <div className="flex flex-col gap-4 p-4">
      {/* TODO: 화면 UI 구현 */}
    </div>
  );
}
