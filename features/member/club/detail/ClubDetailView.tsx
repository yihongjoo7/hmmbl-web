/** [퍼블리셔] ClubDetail 화면 레이아웃 — IA No.100-102 */
// 역할: 정적 안내·설정 화면. props가 필요하면 아래 interface를 채우세요.

interface ClubDetailViewProps {
  // TODO: 필요한 props 정의 (없으면 이 블록 삭제)
}

export function ClubDetailView(_props: ClubDetailViewProps) {
  return (
    <div className="flex flex-col gap-4 p-4">
      {/* TODO: 화면 UI 구현 */}
    </div>
  );
}
