/** [퍼블리셔] LoginRequiredGuide — IA No.29 */
// TODO: 컴포넌트 구현

interface LoginRequiredGuideProps {
  // TODO: props 정의
}

export function LoginRequiredGuide(_props: LoginRequiredGuideProps) {
  return (
    <div className="p-4">
      {/* TODO: UI 구현 */}
    </div>
  );
}
